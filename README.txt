INSTRUCTIONS OF USE

1. Run the 'server.sh' script. This can be done in a dedicated terminal, or as a background process.

2. Run the 'client.sh' script. Each user can run a different instance of this.

3. Use the commands shown by the script, and enjoy! The script will tell you if you used a command incorrectyl, and will inform you on how to use it correctly.
